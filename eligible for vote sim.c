#include<stdio.h>
int main(){
	int n=16,a;
	if(a>=18)
	{
		printf("eligible for vote");
	}
	else
	{
		printf("not eligible for vote\nu will be eligible after %d years",18-a);
}
return 0;
			
}